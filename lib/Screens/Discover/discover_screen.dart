import 'package:flutter/material.dart';
import 'package:example/Core/constants.dart';

class DiscoverMain extends StatelessWidget {
  const DiscoverMain({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'SooOOOooN',
          style: TextStyle(color: forange, fontSize: 25),
        ),
      ),
    );
  }
}
